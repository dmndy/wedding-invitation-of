export function initGifPicker() {
    // Komponen opsional untuk emoji/gif picker
    const gifContainer = document.getElementById('gif-form-default');
    if (gifContainer) {
        gifContainer.innerHTML = ''; 
    }
}